# 数据库工具 - 数据库集成

> Agent 访问数据库

## 🎯 常用数据库

- SQL 数据库
- NoSQL 数据库
- Elasticsearch

---

## 💻 使用示例

```python
from crewai_tools import SQLDatabaseToolkit

# 创建工具
toolkit = SQLDatabaseToolkit(
    db_uri="sqlite:///example.db"
)

# 使用
tool = toolkit.get_sql_tool()
result = tool.run("查询所有数据")
```