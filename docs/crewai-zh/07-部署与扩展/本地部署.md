# 本地部署 - 本地部署

> 在本地部署 CrewAI 应用

## 🎯 部署方式

- 直接运行
- Docker
- Kubernetes

---

## 💻 Docker 部署

```dockerfile
FROM python:3.9-slim
WORKDIR /app
COPY requirements.txt .
RUN pip install -r requirements.txt
COPY .
CMD ["python", "app.py"]
```